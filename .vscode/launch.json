{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Lambda Discipline Extension",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ]
        }
    ]
}
